import 'package:flutter/material.dart';

class AppColors {         //CONTIENE REFERENCIAS A COLORES PARA USAR
  static const Color primary = Colors.deepPurpleAccent;           //Static (todo el mundo puede acceder) const(no se puede cambiar)
  static const Color secondary = Color(0xFF000000);
  static const Color accent = Color(0xFFFDF503);


  //Backgrounds
  static const Color background = Color(0xFF0E0B20);
  static const Color backgroundComponent = Color(0xFF1D1E33);
  static const Color backgroundComponentSelected = Color.fromARGB(255, 79, 84, 139);
}